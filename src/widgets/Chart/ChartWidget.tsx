import React from "react";
import WidgetCard from "../../components/WidgetCard/WidgetCard";

const ChartWidget = () => {
  return <WidgetCard title={"Wykres sprzedaży"} />;
};

export default ChartWidget;
